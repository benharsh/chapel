#!/usr/bin/env python3

import subprocess, sys, os

def genDyno(val):
    dyno = ""
    if val == "<standard>":
        dyno = "chpl_standard.dyno"
    else:
        dyno = val + ".dyno" # TODO: basename
    if not os.path.exists(dyno):
        subprocess.run([sys.argv[3], "testGenLib.chpl", "--dyno-gen-lib", val])

def genAndMod(name):
    dotChpl = name + ".chpl"
    dotDyno = name + ".chpl.dyno"
    genDyno(dotChpl)

    f = open(dotDyno, "r+b")
    data = f.read()

    # Note: length of strings must match.
    prefix = b'MYPREFIX: '
    tmp = " ".join([name, name, name])
    needle = bytes(tmp, 'utf-8')
    rn = name[::-1]
    reverse = bytes("-".join([rn, rn, rn]), 'utf-8')
    updated = data.replace(prefix + needle, prefix + reverse)

    f.seek(0)
    f.write(updated)
    f.flush()

genAndMod("MyMod")
genAndMod("OtherMod")
genDyno("testGenLib.chpl")
genDyno("<standard>")
